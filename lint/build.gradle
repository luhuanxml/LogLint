apply plugin: 'java-library'

dependencies {
    compileOnly deps.lint.api
    compileOnly deps.lint.checks
}

jar {
    manifest {
        attributes('Lint-Registry': 'com.luhuan.lint.ToolKtIssueRegistry')
    }
}